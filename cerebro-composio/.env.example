# Composio API key — get yours at https://app.composio.dev/settings
# Can also be stored in macOS Keychain (the scripts check there as a fallback):
#   security add-generic-password -a composio -s COMPOSIO_API_KEY -w "your_key_here"
COMPOSIO_API_KEY=your_composio_api_key_here
